# vite-plugin-pwa@1.2.0 declares peer vite ^3..^7; we run vite ^8.
# The plugin works at runtime — only the peer range lags upstream.
# Remove this once vite-plugin-pwa publishes a release that includes vite 8 in its peer range.
legacy-peer-deps=true
